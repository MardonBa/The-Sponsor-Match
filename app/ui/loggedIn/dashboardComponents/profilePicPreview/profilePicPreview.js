import styles from './profilePicPreview.module.css';

export default function ProfilePicPreview() {
    return (
        <div className={styles.pfp} >
            {/* 
            container for the user's profile picture 
            Likely need to query the user and get their profile picture, work this out when working on the db
            */}
        </div>
    );
}